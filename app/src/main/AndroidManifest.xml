<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.softsquared.template.kotlin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android:name=android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:name=".config.ApplicationClass"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity
            android:name=".src.splash.SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".src.main.MainActivity"
            android:screenOrientation="portrait" />

        <activity android:name=".src.test.TestActivity">
        </activity>
        <activity android:name=".src.home.HomeActivity">

        </activity>
        <activity android:name=".src.trip.TripActivity"/>
        <activity android:name=".src.flight.FlightActivity"/>
        <activity android:name=".src.go.GoActivity"/>
        <activity android:name=".src.arrive.ArriveActivity"/>
        <activity android:name=".src.date.DateActivity"/>
        <activity android:name=".src.flight_list.FlightListActivity"/>

    </application>

</manifest>